name: Unit Tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: smarttv-unit-tests-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit-tests:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - name: Check out sources
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Typecheck and unit tests with coverage
        run: |
          npm run typecheck
          npm run build
          # Vitest thresholds: 90% stmts/branches/funcs/lines on logic modules
          # (src/api/**, src/storage/**, playback settings, player selection,
          # platform detect). See vite.config.ts coverage.include.
          npm run test:coverage

      - name: Packaging stubs (Tizen / webOS)
        run: |
          npm run build:tizen
          npm run build:tizen-legacy
          npm run build:webos
          test -f dist-tizen/config.xml
          test -f dist-tizen-legacy/config.xml
          grep -q 'package="PrairieApp"' dist-tizen/config.xml
          grep -q 'package="PrairieLte"' dist-tizen-legacy/config.xml
          grep -q 'required_version="2.3"' dist-tizen/config.xml
          grep -q 'required_version="2.3"' dist-tizen-legacy/config.xml
          test -f dist-webos/appinfo.json

      - name: Upload coverage summary
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: smarttv-coverage
          path: coverage
          if-no-files-found: ignore
